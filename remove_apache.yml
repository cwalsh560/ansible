---

- hosts: all
  become: true
  tasks:

  - name:  update repo index
    apt:
      update_cache: yes

  - name: remove apache2 & php package
    apt:
      pkg:
      - apache2
      - libapache2-mod-php
      state: absent
